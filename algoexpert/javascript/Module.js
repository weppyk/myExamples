/** MODULE - see directory Modules
 * in <script type="module"> the code is in strict mode by default
 * Identifiers at the top level are scoped to the module
 * The script will be deferred by default
 * The await keyword can by used at the top level
 * The import keyword can be used to import modules
 * The export keyword can be used to export modules
 * The export default keyword can be used to export modules
 * The import.meta object contains information about the module
 * The import() function can be used to dynamically import modules 
 */

// Immediately Invoked Function Expression
(function(){
    console.log('Hello');
})();